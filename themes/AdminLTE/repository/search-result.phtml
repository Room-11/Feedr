<?php declare(strict_types=1); ?>
<div class="modal fade" id="search-results" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel"><?= $this->translate('modal.repositories.search.result.title'); ?></h4>
            </div>
            <div class="modal-body">
                <table class="table table-hover data-table">
                    <tbody>
                        <?php foreach ($this->repositories as $repository) { ?>
                            <tr>
                                <td><img src="<?= $this->escape($repository['owner']['avatar_url']); ?>" class="user-image" alt="<?= $this->escape($repository['owner']['login']); ?>"></td>
                                <td><a href="<?= $this->escape($repository['html_url']); ?>" target="_blank"><?= $this->escape($repository['full_name']); ?></a></td>
                                <td class="text-right"><button class="btn btn-success btn-flat add-repo" data-id="<?= $this->escape((string) $repository['id']); ?>" data-repo="<?= $this->escape($repository['full_name']); ?>"><i class="fa fa-fw fa-plus"></i></button></td>
                            </tr>
                        <?php } ?>
                    </tfoot>
                </table>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal"><?= $this->translate('modal.repositories.search.result.close'); ?></button>
            </div>
        </div>
    </div>
</div>
