<?php declare(strict_types=1); ?>
<div class="modal fade" id="search-results" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel"><?= $this->translate('modal.repositories.search.result.title'); ?></h4>
            </div>
            <div class="modal-body">
                <table class="table table-hover data-table github-api-result">
                    <tbody>
                        <?php if (!count($this->repositories)) { ?>
                            <tr>
                                <td><?= $this->translate('modal.repositories.search.result.empty'); ?></td>
                            </tr>
                        <?php } else { ?>
                            <?php foreach ($this->repositories as $repository) { ?>
                                <tr>
                                    <td><img src="<?= $this->escape($repository->getOwner()->getAvatarUrl()); ?>" class="user-image" alt="<?= $this->escape($repository->getOwner()->getName()); ?>"></td>
                                    <td><a href="<?= $this->escape($repository->getHtmlUrl()); ?>" target="_blank"><?= $this->escape($repository->getFullName()); ?></a></td>
                                    <td class="text-right"><button class="btn btn-success btn-flat add-repo" data-id="<?= $repository->getId(); ?>" data-repo="<?= $this->escape($repository->getFullName()); ?>"><i class="fa fa-fw fa-plus"></i></button></td>
                                </tr>
                            <?php } ?>
                        <?php }?>
                    </tfoot>
                </table>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal"><?= $this->translate('modal.repositories.search.result.close'); ?></button>
            </div>
        </div>
    </div>
</div>
